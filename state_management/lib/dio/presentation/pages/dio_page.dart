import 'package:flutter/material.dart';

class DioPage extends StatelessWidget {
  const DioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Dio Interceptor"),
      ),
    );
  }
}
